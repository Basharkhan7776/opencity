# Opencity


Like characters random car running on road going any way 5 cars in radius and my vehecle can go through them, use same physics as my vehecle.


Optamization in the characters where we have points but when camera see the characters and vehecle points to generate will generate else not. Also do same optamization with assets like buildings, trees, and other objects.


Map making algo better where path should be smoother and connected roads in race map formation. If we have circle the make a large circle and sprint should accross the map straight.

On esc menu we get the guides for controls.

Make a graphical setting adjustment in the esc menu. resolution(1x, 0.9x, 0.8x, and so on of current resolution) it basically show less pixels, draw distance, number of pedestrians, and other settings.

Always have a random path generated in the race map, where lap have big circle and sprint should mostly straight(means can be shapes like L, U). With difficulty increase map should be harder and more challenging more turns.
