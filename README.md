# Opencity


Like characters random car running on road going any way 5 cars in radius and my vehecle can go through them, use same physics as my vehecle.

Make character smaller and have merage/illusion of straight characters in it.

Optamization in the characters where we have points but when camera see the characters and vehecle points to generate will generate else not. 

we have empty grid (grid fromed by roads) combine the neighbour empty grid cells into larger empty grid cells.

Make alloy wheel or use assets for wheels in car ./assets/vehicles/ different alloy wheels for different vehecles.

Making the racing mechanism where i esc manu have option race select the vehecle, lenght of the race, laps (if 0 laps will on loop path else loop path), only 6 cars will race and diffulcity (easy, medium, hard). From this data every time it make a path for the vehecle to race on we follow the checkpoints and lap the race, my vehecle have arrow on top indicate the direction the direction of ext checkpoint. Every time from this props will generate path for race and checkpoints(for this lenght this checkpoints). dificulty how computer cars will follow the path and checkpoints. we also get meta data in hud position, lap and timing.
